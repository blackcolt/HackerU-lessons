import { UpperactorPipe } from './upperactor.pipe';

describe('UpperactorPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperactorPipe();
    expect(pipe).toBeTruthy();
  });
});
