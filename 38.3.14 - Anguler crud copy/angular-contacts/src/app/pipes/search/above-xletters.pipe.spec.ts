import { AboveXLettersPipe } from './above-xletters.pipe';

describe('AboveXLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new AboveXLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
