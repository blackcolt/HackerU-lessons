 - Connect mysql via vscode.
 - CREATE, DROP, ALTER, SHOW
 - AND OR 
 - WHERE IN
 - Two keys as uniqe.
 - Join
 - field AS
 - Connect mysql via command line
    cd c:\xampp\mysql\bin
    mysql.exe -u root --root